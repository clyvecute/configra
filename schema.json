{
	"version": 1,
	"rules": {
		"app_name": { "type": "string", "required": true },
		"max_retries": { "type": "int", "min": 0, "max": 5 },
		"mode": { "type": "enum", "allowed": ["debug", "release"] }
	}
}
